

## Rackspace server `hot_mess`

* Root password `enEYGghGVD3U`
* Using public key `infirno`

To update the server run the script `update.sh` in `/srv/http/hot_mess` and restart the service by running `systemctl hot_mess-web-1`

Alternate way to reboot puma via pumactl command.
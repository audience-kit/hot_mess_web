= form_for [:admin, @locale] do |form|
  .form-group
    = form.label :name
    = form.text_field :name, { class: 'form-control' }

  .form-group
    = form.label :dns_name
    = form.text_field :dns_name, { class: 'form-control' }

  = form.submit 'Create', { class: 'btn btn-success' }
